apiVersion: 1
providers:
  - name: Agent Workflow
    folder: Agent
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/dashboards
