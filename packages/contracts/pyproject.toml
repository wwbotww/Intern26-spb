[project]
name = "spb-contracts"
version = "0.1.0"
description = "离线政策流水线与在线 RAG API 之间的稳定数据契约"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/spb_contracts"]
