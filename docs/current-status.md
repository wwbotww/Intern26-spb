# 当前交付状态

原交付核对日期：2026-09-10，代码审查基线：`a2fb398`；价格本地开发更新于 2026-09-11，见下文。
本文是能力状态的唯一汇总入口。
部署版本与代码 HEAD 分开记录；没有新验收证据时，不因代码提交而自动标记上线。

## 阶段结论

**“受约束 Agent + 单实例内网交付”已阶段性完成，可以结束逐阶段开发日志式维护。**
这不等于三类物流真实接口、代表性模型质量、全品类价格或企业级多副本已经完成。
后续按[路线图](roadmap.md)拆成独立任务，不再把所有残余工作挂在“阶段 6 未完”之下。

| 交付范围 | 状态 | 验收边界 |
| --- | --- | --- |
| LangGraph Kernel、Hybrid Understanding、类型化路由 | 已实现 | 规则优先、可选模型、硬实体复验；不是自由 ReAct |
| 补槽、冲突确认、有界重试、SQLite、三层幂等 | 已实现并回归 | 单实例；不保证任意崩溃窗口的 exactly-once |
| V2 JSON/SSE、领域卡片、访客身份、刷新与重放 | 已实现 | SSE 为业务生命周期流；匿名身份不是企业登录 |
| 政策 RAG、设备价格 V1 复用 | 已接入真实依赖 | 正常无匹配/证据不足与服务不可用分开 |
| 查轨迹 T0～T3 | 本地 / Mock 完成 | 已有协议 Adapter、受控装配及 Web；T4 真实互通未验 |
| 查资费 P0～P3 | 本地 / Mock 完成 | 只允许合成 transport；P4 仍需合同确认及真实装配 |
| 查时限 | 领域 / Fake 通路完成 | 尚无供应商文档、wire Adapter 或真实结果 |
| Eval、故障矩阵、审核冻结、语义 Trace、逐 Node OTel | 工程工具完成 | 独立真实 holdout、线上监控与 SLO 未验 |
| 单实例部署、整库恢复、V1 回退演练、远程 CI | 已验收并发布限定内网版本 | HTTP 风险显式接受；非公网生产安全认证 |
| 首页通知与示例修正 | `a2fb398` 已提交、本地验证 | Web 89 / Python 1089；上线版本须另核对私有发布记录 |

## 当前业务通路

| 用户需求 | 已核验状态 | 剩余限制 |
| --- | --- | --- |
| 政策查询 | 真实 RAG → 引用式回答或有依据拒答 | 资料适用性、有效期、企业办理指南覆盖仍需治理 |
| 设备价格 | 真实 MySQL V1 → 型号/规格约束 → 候选卡片 | 当前目录不覆盖所有历史型号；参考价不等于成交价 |
| 轨迹 / 资费 / 时限 | 正式部署均正常显示暂不可用 | 不补收无法执行所需的单号、地址或重量，不加载 Fake |
| 全品类价格 V2 | A～D7 本地通路与回归已完成；原库 V2 已通过有限只读样本合同；正在执行限定范围发布门禁 | 完整五品牌语义、最小权限生产账号、可选历史及旧 SQL 清理仍属 E 后续，不将本地通过标为上线 |

2026-09-10 的只读复查已确认设备库 `product=103`、`sku=1114`、`price_current=1114`；
“价格库为空”只是当天较早发布时的快照，不是当前结论。数据会变化，以上不是实时监控。
数据职责与后续适配见[数据源边界](data-sources.md)。

## 价格 V2 改造：A / B 提交与 C 本地交付

2026-09-11，基于 `9efb621` 工作区实施[价格迁移计划](product-price-implementation-plan.md)，
[ADR-0020](adr/0020-unified-product-price-query.md)已采纳。A/B 随后提交为 `5b2e637`，未推送/发布，也不是新线上核验。

- A1：固定 27 个有效设备行为，在旧 Tool/Agent 兼容适配器两条路径复用同一合成基线。
  发现 Pro→Pro Max 替代及基本款混入 Pro 两类既有缺陷，保存为 4 个 strict xfail，不作为正确行为接受。
- A2/A4：完成[消费合同](../apps/assistant-api/docs/integrations/product-price.md)本地局部冻结、
  类型化读取事实、设备/生鲜/无金额夹具及 manifest；校验金额、单位、关联、状态、来源日和历史边界。
  采集侧源码快照有未提交修改，正式 commit、生产迁移、建档/状态推进及真实覆盖仍待交接。
- A3：明确新槽位/结果、候选输入、客户端契约门禁及旧历史处理设计；
  **公开 API、Intent、Graph、State、Web 和生产装配未改变**，现有价格查询仍读 V1。

阶段 A 验证：当时工作树全量 Python 离线回归 `1352 passed, 4 xfailed`；
新增部分为 207 个事实合同测试、55 个有效基线/完整性测试和 1 个 manifest 测试。
预期失败对应上述两类已有缺陷的双入口，不是新 V2 实现已修复；未访问真实业务数据库或外部服务，未调用付费模型。

### B：V2 只读核心与 SQL 门禁

同日继续基于 `9efb621` 工作区实现，最终包含在 `5b2e637`；下列是 B 当时的验收快照：

- B1/B2：新增独立 Domain 查询类型、只读 Port、固定 V2 Repository 与共享 Query Service。
  设备先产品再规格有界召回，生鲜不依赖标准商品建档；绑定参数、稳定顺序和截断标记明确。
  此时返回候选事实，不是已完成 C 的自然语言硬匹配和最终报价。
- B3：校验当前指针、版本、可信匹配、金额/单位、无金额状态与来源时间；
  显式来源级 freshness 阈值可配置，默认 `unknown`，测试阈值不当作已确认的真实业务策略。
  生鲜共享数据集绑定来源日及原生商品 ID；来源 URL 使用选中观察的 crawl 证据。
  **历史合同已支持，但历史 SQL 保守不返回**，记为 PRICE-G11，不把旧金额补成当前价。
- B4：实际必需投影的 schema/权限探测、稳定失败分类、只读一致事务；取消后等待线程真实退出才归还容量。
  并发关闭复用一次清理任务，Service 只借用 Repository。应用组合根切换在 C，现有线上装配不变。
- B5：新增[专用 MySQL 门禁](../deploy/price-query/README.md)与 CI 独立矩阵。
  固定镜像、新建本地回环容器、合成数据与 V2 SELECT-only 账号；无 V1 表、V1 存在但无权限两种情形均验证。

B 阶段验证：

| 验证 | 结果与边界 |
| --- | --- |
| 全量 Python 离线回归 | `1569 passed, 45 skipped, 4 xfailed`；45 项真实 SQL 在普通 pytest 中按设计跳过 |
| MySQL 5.7.36 隔离 runner | 50 项通过：45 个真实 SQL 场景 + 5 个安全配置场景 |
| MySQL 8.4.11 隔离 runner | 同上 50 项通过；不是业务库、生产迁移或性能规模验收 |
| CI 认证依赖隔离 | 8.4 的 bootstrap root 与测试账号显式使用测试认证策略；禁用本机 PyMySQL 可选 RSA 能力后仍通过，未增加 API 依赖 |
| 并发写入快照 | 产品选择后 writer 改价，规格查询仍读取事务原事实；下一次查询才见新值 |
| 权限与测试隔离 | reader 写入和读取旧表均被数据库拒绝；额外 26 项离线 guard 验证回环 DSN、本次库/角色及 Docker endpoint |
| CI / 发布 | 已配置新 job，**尚未推送或验证远程运行**；API、State、OpenAPI 与 Web 未改变 |

测试结束已按本次容器完整 ID/标签清理可重建的 tmpfs 合成数据；现有采集容器及业务数据库未操作。
当时 4 个 strict xfail 对应 Pro→Pro Max 和基本款混入 Pro 两类旧缺陷的双入口，不是 B 新故障；现已在 C 修复。

### C：设备硬匹配、统一报价与受控薄适配

2026-09-11，基于 `5b2e637` 继续实现；C 为工作区未提交改动，未推送、未修改生产配置或部署：

- C1：Parser 迁至 Domain，产品硬约束/排序/规格策略由新旧路径共享；修复两类型号误匹配，
  4 个 strict xfail 转为正常通过。A 的 Gold 和 fixture 摘要未改，PRICE-G09/G10 仅关闭本地实现缺口。
- C2：统一 Service 提供设备报价，保留原价未知、官方 SKU 可空、当前无金额事实、来源时间与 freshness。
  内部结构化筛选明确区分容量/内存及其他已证明属性；新自然语言槽位仍待 D。
  区分召回与展示截断；不能因为截断后只剩状态记录，就断言不存在数值报价。
- C3：显式 `catalog_v2` 配置使用应用持有的唯一 Repository/Service，V1 包装器和既有 Agent 设备适配器借用。
  启动失败也清理已分配的价格/政策资源；缺配置、故障或无匹配都不 fallback V1。默认 `device_v1` 保持不变。
  旧卡片不能表达无金额：内部保持状态命中，旧协议以原因码和正文保留事实、价格证据留空；具体限制见消费合同。
- C4：冻结的 27 个有效场景和 2 个原缺陷，分别通过 V2 包装器和 Agent 适配器完成同证据对照。
  比较字段与原因，不比较不同抓取日价格；实际 V2 SQL 也经过统一报价与两个旧协议面验证。
- C5：旧 SQL/Record/Tool、Demo 和历史解码器的依赖清单写入消费合同；仅保留默认旧发布窗口所需路径，
  不在 C 提前删生产旧 SQL。D 同步 Demo/客户端/状态，E 再清理已无生产依赖的旧查询。

本轮验证：

| 验证 | 结果与边界 |
| --- | --- |
| 全量 Python 离线回归 | `1724 passed, 47 skipped`；无 xfail，47 项真实 SQL 按设计仅在专用 runner 执行 |
| V2 同证据等价 | 58 个双入口场景 + 1 个 fixture 完整性测试通过 |
| C 报价/边界 | 35 项通过，包含空金额、混合状态、规格角色、截断、旧协议错误分类及取消 |
| C 受控装配 | 28 项通过，包含共用资源、启动/关闭异常、无 V1 fallback；使用合成 HTTP/状态库 |
| MySQL 5.7.36 / 8.4.11 | 各 52 项通过：47 个真实 SQL 场景 + 5 个安全配置场景；包含新增的 V2 报价和旧协议适配 |
| 公开合同与格式 | `check:agent-types`、架构/现有 API 合同和格式检查通过；未改 OpenAPI/State/Intent/Web，无新远程 CI 结果 |

本轮隔离容器及可重建测试数据已清理，没有修改原采集数据库、RAG 或服务器。
C 后续按 D1/D2 → D3～D7 推进，以下记录 D1/D2 本轮结果。
客户端/状态门禁完成前，不对现有持久 Agent 会话切换 catalog_v2；发布及可选历史仍按 E/缺口表推进。

### D1/D2：统一理解与条件合并，尚不公开新 HTTP 能力

2026-09-11，在 C 未提交工作区上继续实现；本轮未提交/推送、未部署、未改 `.env` 或数据，也未调用付费模型。

- 新增 `product_price` 意图、设备/生鲜/未知类目联合槽位、类型化 Command 和组件能力描述。
  分离容量与内存、商品计价数量与寄递重量、来源地区与物流行政代码；保留单位和时间请求，不算出模型价格。
- 规则/模型 profile 与独立 Eval 同步。重量不独立路由资费，保留寄递补重量、否定纠错和多意图行为。
  模型只补语义分类，硬实体重提；profile 不符或伪造执行标识在模型边界拒绝，并正确记录失败。
- 嵌套条件原子合并；确认改类目/品牌/型号/品名/地区后清理依赖规格/品种/单位/市场。
  语义变化输出候选失效标志，同义单位原文不改变语义指纹；实际候选和状态消费在 D3。
- OpenAPI/生成 TS 只保留新通用 Intent 枚举；公开能力、结果类型和卡片未开放。
  HTTP 显式输入与依赖注册都有阻断，默认理解/生产装配仍走旧 profile；不是可提前打开的新生产开关。
- 新增独立 24 条合成 development，未改变原 48 条样本及 A 的设备 Gold。
  详情、实现和测试见[商品价格 Understanding](../apps/assistant-api/docs/integrations/product-price-understanding.md)。

本轮验证：全量 Python `1813 passed, 47 skipped`（47 项隔离 SQL 按设计不在普通 pytest 运行），
Web `90 passed`，Agent/V1 两模式 `vue-tsc + Vite build`、生成类型检查和聚焦 Ruff 检查通过。
24 条新 development 经独立导出/评分逐条通过；原 48 条规则对照得分仍保留，未引入新的真实模型实验。
本轮没有复跑两个隔离 MySQL runner（其 C 阶段结果见上文）；真实公司库只做下述只读投影/样本检查。
组件测试不代表全品类 HTTP 工作流或模型泛化质量已通过。

### 原价格库只读核对：数据存在，但不是正式切换验收

同日按用户确认复用原服务器部署配置。发现本机 Assistant 示例 DSN 的主机仍为 `mysql-host`，
不能用于真实连接；没有覆盖它或复制生产凭据。原 API 容器配置可连接目标价格库，实查
MySQL `5.7.36-log`、迁移 `b72c910e4f31`。一次连接读取超时后重试成功，不把该故障解释为数据为空。

| V2 当前指针来源 | 数量 |
| --- | ---: |
| Apple | 312 |
| 华为 | 460 |
| 小米 | 138 |
| OPPO | 110 |
| vivo | 115 |
| 商务部批发 | 1,354 |
| 上海零售 | 8 |

设备合计 1,135 条；生鲜合计 1,362 条。这是一次 current 指针快照，不等于有效型号覆盖率或所有价格语义都通过。
设备最新观察来自 9 月 11 日；商务部/上海观察的 UTC 时点分别仍在 8 月 22 日/24 日 16:00，
对应中国时区来源日 8 月 23 日/25 日，不宣称今天实时价。

当前消费者代码仅以内存源码运行于旧容器的独立进程，未落盘或替换服务。
设备/生鲜实际必需投影探测通过；有界 SQL+严格行映射分别验证 Apple/华为/OPPO/vivo 各 2 条、
零售 1 条、批发 2 条。小米 `redmi` 产品名搜索返回 0 条（正常无匹配），不能据此关闭小米语义覆盖验收。
无自动 V1 查询/回退、未运行公司库集成测试、未改账号或 schema。

旧容器独立探针进程无法创建工作线程，因此采用同一 Repository 同步 SQL/映射边界检查；
这不证明新异步 Runtime 能直接在旧资源限制下运行。PRICE-G13 留待部署前复核，未调高资源限制。
采集侧最新 M 文档仍报告 6 组 iMac 重复来源身份待限定修复；本轮没有核改这些行，也不代替生产者关闭验收。
V2-only 最小权限、完整五品牌同证据行为、无金额状态与历史的真实范围仍按 E/缺口表完成。

### D3：商品 Tool 与内部候选闭环

2026-09-11，在上述 C/D1/D2 工作区上继续开发，未提交、推送、访问生产库或更新部署：

- ProductPriceTool 借用共享 V2 Service；设备沿用硬身份/规格匹配，生鲜使用独立来源地区目录和条件过滤。
  上海零售 CITY 与商务部批发 PROVINCE 分开；品名、市场/来源规格不随意放宽，区县不缩成全市报价。
- LangGraph 校验后的 need_more_info 返回决策/澄清；每价格 query 限 2 次逻辑调用、1 次技术重试、3 轮澄清。
  选择、普通补槽和 pending 上的新消息不刷新额度；其他业务仍保持原预算。
- 不透明候选 token 绑定 owner/conversation/query/约束/有效期；条件变化清理候选。
  选择后用固定 SQL 精确重读同 listing/地区当前指针；身份/revision 变化或指针消失时不替换商品。
- 发现与最终报价分别保存收据；同 call 重放保留原事实，新 query 重新读库。
  内存与 SQLite 关闭重开恢复测试通过；不是旧版本 State 迁移或公开消息级幂等验收。
- 无金额是状态命中，不造零元。今日请求若只有旧观察返回 partial 和明确 reason，原始/标准单位及来源日期不改写。
- D3 仅显式内部 Runtime 注入，现有 HTTP 能力注册、公开投影与新价格原始流式入口仍阻断。
  内部 ProductPriceData 含数据库标识，不能直接用作 D4 公开 DTO；当前生产 State 版本仍为 3。

本轮验证：全量 Python `1866 passed, 51 skipped`；51 项仅由专用隔离 SQL runner 运行。
MySQL `5.7.36` / `8.4.11` 各 `56 passed`，包含精确候选读取与当前指针推进/消失，
只操作本次新建的合成测试容器，运行结束已删除各自容器与 tmpfs 数据。
Web `90 passed`、生成类型检查、Agent/V1 两模式 `vue-tsc + Vite build` 通过。
没有新增付费模型调用或新的真实覆盖结论。
实现和测试边界见[商品价格执行循环](../apps/assistant-api/docs/integrations/product-price-agent-loop.md)。

### D4～D7：公开价格、Web 与状态升级

2026-09-11，本轮继续完成 API/Web/Eval 的配套交付，发布结果另记在下方，不提前推定上线。

- 应用受控 catalog_v2 装配新价格意图；公开白名单不含事实 ID/指纹，保留金额/状态、单位、来源与时间。
- 结构化候选输入纳入消息幂等，JSON/SSE 共用原事实；浏览器契约门禁在流式/写入前拒绝旧页面。
- State 4 保留旧已完成结果和收据，旧价格 pending 安全要求重启；只读 owned snapshot 不执行 Graph/续期。
  备份 profile 同步升级并接受旧 v3 备份；Nginx 精确开放 GET 快照，其他方法继续拒绝。
- 通用价格卡片、候选点击和历史恢复完成。浏览器验收发现并修复“批发或零售口径”表单标签污染语义，
  新增 Web 消息生成及公开 API 补槽回归；来源日、无金额和不跨来源混算均有独立断言。
- 7 条 / 9 轮合成商品 public development 加入默认 CI，同时保留原资费 13 条 / 28 轮；不是 holdout 或模型质量结果。

最新本地结果：Python `1891 passed, 51 skipped`；两版隔离 MySQL 各 `56 passed`；Web `114 passed`；
生成类型、vue-tsc、Agent/V1 双模式构建、Ruff 关键错误和 whitespace 门禁通过。
浏览器实测设备候选→刷新→点击报价、生鲜补槽→报价→刷新历史；未新增付费模型实验。
完整协议和发布顺序见[协调发布设计](../apps/assistant-api/docs/integrations/product-price-public-release.md)。

下一步是匹配提交的远程 CI、发布产物及原服务器停服备份/新目录恢复/目标核验。
部署前已只读确认旧 API 基于 `6a7a79a`、Web 实际为 `a2fb398`；原 RAG、MySQL、模型配置复用。
限定发布不关闭 PRICE-G02/G03/G05/G06/G07/G11/G12/G14 的完整覆盖与治理事项。

## 部署与证据

- 已确认的内网发布：API 产物基于 `6a7a79a`，Web 基于 `580908c`；`277cfba` 为收尾文档。
  后续 `a2fb398` 的首页更新不能凭 Git 状态推定已切换服务器。
- 正式入口复用原 RAG / MySQL，不重建或导入业务数据；Agent 状态另用 managed SQLite。
- 原 HTTP 入口仅在明确批准的私有 IPv4 范围运行。签名 Cookie / 同源校验不提供链路加密。
- 发布时通过外部 HTTP、Cookie、JSON/SSE 和双访客隔离检查；当时浏览器自动操作连接超时。
  后续首页修复在本地合成浏览器验证，不替代目标页面完整人工验收。
- 历史 CI、模型实验、发布镜像摘要与数据快照集中于[交付摘要](history/agent-delivery-summary.md)。
  当前复跑方式见[开发指南](development.md)，不要复制阶段测试计数充当最新结果。

## 完成与未完成的判断规则

1. 代码存在、Mock 通过、供应商互通、部署、业务质量是不同验收层级，分别标记。
2. `capabilities.available` 表示已装配，不保证上游在线；readiness 不保证任意问题可回答。
3. development 满分不能写成泛化准确率；工程测试数量不能写成业务成功率。
4. 需要合同、数据、真实调用授权的任务保持待办，不通过虚构配置或合成数据“关闭缺口”。
5. 状态更新同时记录日期、代码或部署版本和证据位置；其他文档引用本文，不重复记进度。
